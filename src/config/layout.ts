// src/config/layout.ts

// 配置: 布局常量

/** 布局头部的常量 */
export const HEADER ={
    /** 头部高度(以像素为单位) */
    HEIGHT_PIXEL: 50
}

/** 布局侧边栏的常量 */
export const SIDER ={
    WIDTH: 200
}

/** 布局内容部分的常量 */
export const CONTENT ={
    PADDING: 10
}

/** 用户管理部分的常量 */
export const USER ={
    AVATAR: {
        WIDTH: 36,
        HEIGHT: 36,
    },
    /** 顶部高度(以像素为单位, 包括标签区域和标题栏) */
    TOP_HEIGHT_PIXEL: 122,
}

/** 动态管理部分的常量 */
export const DYNAMIC ={
    /** 顶部高度(以像素为单位, 包括标题和日期标签区域) */
    TOP_HEIGHT_PIXEL: 120,
}